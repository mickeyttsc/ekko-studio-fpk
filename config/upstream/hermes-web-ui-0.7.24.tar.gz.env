# 上游 hermes-web-ui 预构建产物的固定校验值（入库，由 auto-update.yml 自动更新）
#
# 为什么固定进版本控制：只做「现拉 sidecar 现比」挡不住上游事后替换同版本
# 资产（sidecar 会跟着一起变，等于没校验）。固定值入库才能发现变更。
# build-fpk.sh 优先读这里的 SHA256，没有才退回拉取上游 <asset>.sha256 sidecar。
HERMES_WEB_UI_VERSION=0.7.24
HERMES_WEB_UI_SHA256=515aeccc826eada3abce4be888b63d957f27ee0c5ac86fa6fedebc09f14b7797
