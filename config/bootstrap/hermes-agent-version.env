# 上游版本锚点（由 .github/workflows/auto-update.yml 自动跟踪更新）

# Hermes Agent 上游 release 标签（日期式，如 v2026.9.24）。
# 构建时按此标签下载离线源码包，保证可复现、与上游发版对齐；
# 标签下载失败会回退 main（代价是失去该次可复现性，仅作兜底）。
HERMES_AGENT_VERSION=v2026.9.24
